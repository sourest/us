

enum types {
  CACHE_USER = 'CACHE_USER'
}

const initValue = {}

const model = (initValue, action) => {
  switch (action.type) {
    case types.CACHE_USER:
      
      break;
  
    default:
      break;
  }

}

export default model